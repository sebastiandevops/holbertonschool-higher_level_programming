#!/usr/bin/python3
def list_division(my_list_1, my_list_2, list_length):
    try:
        new_list = my_list.copy()
        for num in range(len(new_list)):
            if new_list[num] % 2 == 0:
                new_list[num] = True
            else:
            new_list[num] = False
    return new_list

    except:

    finally:

